rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /uploads/{allPaths=**} {
      allow read: if request.time < resource.metadata.expiresAt;
      allow write: if request.resource.size < 5 * 1024 * 1024 * 1024;
    }
  }
}